//
//  ARCHIndicationViewProxy.swift
//  HHModuleDemo
//
//  Created by basalaev on 30.08.2018.
//  Copyright © 2018 HandH. All rights reserved.
//

import UIKit

/*
public class ARCHIndicationViewProxy: ARCHIndicationView, ARCHIndicationViewProvider {

    public let views: [ARCHIndicationView]

    public init(views: [ARCHIndicationView]) {
        self.views = views
    }

    public func showIn(container: UIView, layoutGuide: UILayoutGuide, animated: Bool) {
        views.forEach({ $0.showIn(container: container, layoutGuide: layoutGuide, animated: animated) })
    }

    public func removeFrom(container: UIView) {
        views.forEach({ $0.removeFrom(container: container) })
    }

    public func update(state: Any?) {
        views.forEach({ $0.update(state: state) })
    }

    public func typeExist(state: Any?) -> Bool {
        return views.reduce(false, { $0 || $1.typeExist(state: state) })
    }
}*/
