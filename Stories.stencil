import HHModule

internal struct GenStories: ARCHModuleID {
{% for type in types.based.ARCHModuleConfigurator %}
    internal static var {{ type.localName|lowercase }}: {{ type.name }}.Type = {{ type.name }}.self
{% endfor %}
}